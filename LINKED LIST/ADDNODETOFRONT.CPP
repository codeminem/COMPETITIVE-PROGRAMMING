#include<iostream>
using namespace std;
/*void push(Node** head_ref ,int new_data)
{
        //Allocate node
        Node * new_node=new Node();
        //put data in the node
        new_node->data=new_data;
        //make next of node as the head 
        next_node->next=(*head_ref);
        //mpve the head to point to the new node
        (*head_ref)=new_node;

}*/